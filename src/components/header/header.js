import { Link } from 'gatsby'
import PropTypes from 'prop-types'
import React from 'react'
import './header.scss'

const Header = () => (
  <div>
  </div>
)

Header.propTypes = {
}

Header.defaultProps = {
}

export default Header
