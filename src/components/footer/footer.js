import { Link } from 'gatsby'
import PropTypes from 'prop-types'
import React from 'react'
import './footer.scss'

const Footer = () => (
  <div>
  </div>
)

Footer.propTypes = {
}

Footer.defaultProps = {
}

export default Footer
