import { Link } from 'gatsby'
import PropTypes from 'prop-types'
import React from 'react'
import './button.scss'

const Button = () => (
  <div>
  </div>
)

Button.propTypes = {
}

Button.defaultProps = {
}

export default Button
